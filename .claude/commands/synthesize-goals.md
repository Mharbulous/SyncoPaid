Run the goal-synthesis skill to generate/update goals documentation.

Use the Skill tool to invoke the `goal-synthesis` skill, which will synthesize the project's goals and non-goals from the story-tree database.
