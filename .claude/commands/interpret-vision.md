Run the interpret-vision skill to generate/update vision documentation.

Use the Skill tool to invoke the `interpret-vision` skill, which will synthesize the user's product vision from the story-tree database.
