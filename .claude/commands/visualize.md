Run the visualization skill to generate/update vision documentation.

Use the Skill tool to invoke the `visualization` skill, which will synthesize the user's product vision from the story-tree database.
