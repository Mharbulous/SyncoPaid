Invoke the streamline skill to identify and decompose oversized source files.

**Arguments:** `$ARGUMENTS`

Use the Skill tool to run the 'streamline' skill with the provided arguments.

- No arguments: Interactive mode (presents plan, waits for approval)
- "yolo": Autonomous mode (auto-selects, auto-approves, commits and pushes)
- File path: Target specific file for decomposition
